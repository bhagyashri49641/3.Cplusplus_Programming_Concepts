using namespace std; //to use standerd library functions 
// used to reduce ambiguity for same identifier names
#include<iostream> // header file contains declarations for the identifier cout and the operator <<(Operator overloading) 

int main() //entry point function
{
	cout<<"Shree Ganesh!!!\n"; // cout is a object of output stream class
	// << is extraction operator
	//<< inserts the contents of the variable on its right to the object on its left
	return 0;
}
